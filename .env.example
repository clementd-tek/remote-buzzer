# Copy to .env and adjust as needed.

# Origin the React frontend will be served from. Used for CORS on the
# REST API and for validating the websocket handshake's Origin header.
FRONTEND_ORIGIN=http://localhost:5173
